import sys
import numpy as np 
import matplotlib
#Creating a neuron
                            #these are 3 outputs from the previous layer, we use as our new inputs
input = [2.6, 1.2, 7.7]    
weights = [3.9, 0.4, 3.6]     #Each input also have a unique weight
bias = 3    #Every unique neuron has a unique bias

#Now, lets add up all the inputs, times the weights, plus the bias
output = input[0] * weights[0] + input[1] * weights[1] + input[2] * weights[2] + bias
print(output)
